# Expo's dependency graph carries optional peer deps (e.g. a floating react-dom
# that wants a newer react than the SDK pins). legacy-peer-deps lets npm install
# resolve the tree the way Expo intends instead of erroring on those peers.
legacy-peer-deps=true
